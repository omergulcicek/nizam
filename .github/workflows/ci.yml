name: CI

on:
  push:
    branches: [main, master]
    paths:
      - "templates/**"
      - "templates-i18n/**"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "templates/**"
      - "templates-i18n/**"
      - ".github/workflows/ci.yml"

jobs:
  templates:
    name: Template CI (${{ matrix.template }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        template:
          - templates/nextjs
          - templates/tanstack
          - templates-i18n/nextjs
          - templates-i18n/tanstack

    defaults:
      run:
        working-directory: ${{ matrix.template }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: ${{ matrix.template }}/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Paraglide
        if: contains(matrix.template, 'i18n/tanstack')
        run: pnpm exec paraglide-js compile --project ./project.inlang

      - name: Typecheck
        run: pnpm run typecheck

      - name: Lint
        run: pnpm run lint:ci

      - name: Knip
        run: pnpm run knip

      - name: Prepare environment
        run: cp .env.example .env.local

      - name: Build
        run: pnpm run build
